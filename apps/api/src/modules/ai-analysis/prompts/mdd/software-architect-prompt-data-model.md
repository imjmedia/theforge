## Salida — pasada `data_model` (§3 únicamente)

Responde **solo** con el cuerpo de **## 3. Modelo de Datos** (SQL PostgreSQL + TechnicalMetadata; el erDiagram lo genera el pipeline). **PROHIBIDO** devolver el MDD completo, §4–§7 ni repetir §1–§2 en la salida. Usa el contexto de referencia solo para coherencia.

Alternativa aceptable: JSON con markdown de §3 o `sqlSchema`.

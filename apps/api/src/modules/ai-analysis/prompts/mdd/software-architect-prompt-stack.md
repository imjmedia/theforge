## Salida — pasada `stack` (§2 únicamente)

Responde **solo** con el cuerpo de **## 2. Arquitectura y Stack** (heading `## 2. …` opcional). **PROHIBIDO** devolver el MDD completo, §1, §3–§7 ni copiar secciones enteras del contexto de referencia. El pipeline conserva §1 y hace merge quirúrgico.

Alternativa aceptable: JSON `{ "arquitecturaStack": "<markdown §2>" }`.

## Salida — pasada `api_contracts` (§4 únicamente)

Responde **solo** con el cuerpo de **## 4. Contratos de API** (tabla resumen GFM + endpoints con request/response en bloques ```json). **PROHIBIDO** devolver el MDD completo, §5–§7 ni repetir §1–§3 en la salida. Alinea rutas y payloads con el SQL de referencia en §3.

Alternativa aceptable: JSON `{ "contratosApi": { "summary": "<markdown §4>" } }`.

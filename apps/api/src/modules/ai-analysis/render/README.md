# AI Analysis / Render

Renderizado de MDD: conversión de schema estructurado (JSON) a markdown final.

- **`mdd-structured-to-markdown.ts`** — transforma `MddStructured` → markdown con secciones canónicas.
- **`json2md.d.ts`** — tipos para el renderizado JSON→MD.
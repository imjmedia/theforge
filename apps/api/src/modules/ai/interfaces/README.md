# AI / Interfaces

Contratos TypeScript para el módulo AI.

- **`llm-provider.interface.ts`** — `LLMProvider`: interfaz que todo adapter de proveedor debe implementar (`generateResponse`, `generateResponseStream`, `parseChecklist`, `generateEmbedding`).
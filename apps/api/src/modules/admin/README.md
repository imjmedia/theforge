# Admin

Endpoints de admin/supervisor para monitoreo y diagnóstico del sistema. Acceso restringido a `super_admin`.

## Endpoints

- **`GET /admin/mcp/tools`** — lista las tools MCP registradas en el servidor (debug/dev).
- **`GET /admin/health`** — health check del servidor.